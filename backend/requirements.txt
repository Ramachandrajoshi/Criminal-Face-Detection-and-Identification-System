# FastAPI
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# ML / Face Analysis
deepface>=0.0.100
# Pin TF to 2.16.1 to match the tensorflow/tensorflow:2.16.1-gpu base image.
# That image ships cuDNN 8.9 (system lib). Upgrading TF to 2.17+ would require
# cuDNN 9 via the nvidia-cudnn-cu12 Python package, which is not present in
# the base image, causing "Cannot dlopen some GPU libraries" at runtime.
tensorflow==2.16.1
tf-keras==2.16.1
opencv-python>=4.9.0
numpy>=1.24.0
pillow>=10.2.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
cryptography>=41.0.0

# Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Utilities
python-multipart>=0.0.6
aiofiles>=23.2.1
httpx>=0.26.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
